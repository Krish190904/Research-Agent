version: '3.8'
services:
  agent:
    build:
      context: .
      dockerfile: Dockerfile.lite
    ports:
      - "8501:8501"
    volumes:
      - ./:/app
    environment:
      - PYTHONUNBUFFERED=1
