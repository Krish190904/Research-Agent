streamlit>=1.25.0
scikit-learn>=1.3.0
sqlalchemy>=2.0.0
beautifulsoup4>=4.12.0
python-frontmatter>=1.0.0
markdown2>=2.4.0
typer>=0.3.0
tqdm>=4.65.0
requests>=2.25.0
pytest>=7.4.0

# Note: This lite list intentionally omits heavy ML libs (torch, transformers,
# sentence-transformers, faiss-cpu, spacy, weasyprint). Use the full
# `requirements.txt` when building a full image (INSTALL_HEAVY=true).
